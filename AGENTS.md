# AGENTS.md

## 项目定位

`macrossx-tools` 是一个通过 GitHub Pages 发布的在线工具集合。

- 一个工具对应一个独立源码目录。
- 所有工具应当打开即用，优先在浏览器本地完成处理。
- 首页负责展示、分类、搜索和链接所有工具。
- 项目默认面向中文用户，同时兼顾桌面端和移动端。

## 技术栈

项目统一使用以下技术：

- Vue 3
- Vite
- TypeScript
- Vue Router
- Tailwind CSS 4
- shadcn-vue
- Reka UI
- Lucide Vue（`@lucide/vue`）

开发约定：

- 使用 Vue Composition API 和 `<script setup lang="ts">`。
- 使用 Vite 构建，不引入 Nuxt、SSR、数据库或后端服务。
- 默认使用 Hash 路由，确保 GitHub Pages 刷新和直接访问不会出现 404。
- 不随意增加新的 UI 框架、CSS 框架、状态库或工具库。
- 只有出现明确的跨页面复杂状态时才考虑 Pinia；普通工具优先使用组件状态和 composable。
- 页面必须适配 GitHub Pages 的项目子路径 `/macrossx-tools/`，禁止依赖域名根路径。

## 目录约定

```text
macrossx-tools/
├── src/
│   ├── assets/                 # 全站样式和静态资源
│   ├── components/
│   │   ├── ui/                 # shadcn-vue 基础组件
│   │   ├── ToolCard.vue
│   │   ├── ToolLayout.vue
│   │   └── ToolSearch.vue
│   ├── composables/            # 可复用组合式逻辑
│   ├── lib/                    # 无界面工具函数及配置
│   ├── router/
│   ├── tools/
│   │   └── <tool-name>/        # 一个工具一个目录
│   │       ├── <ToolName>.vue
│   │       ├── index.ts
│   │       └── components/     # 仅该工具使用的组件
│   ├── views/
│   │   └── HomeView.vue
│   ├── App.vue
│   └── main.ts
├── public/                     # 无需构建处理的静态文件
├── index.html
├── README.md
└── AGENTS.md
```

- 工具目录名使用简短、清晰的英文 `kebab-case`，例如 `json-formatter`。
- 工具组件使用 PascalCase，例如 `JsonFormatter.vue`。
- 多个工具共用的组件放入 `src/components/`。
- 只被单个工具使用的组件和逻辑保留在该工具目录内。
- 无界面的纯函数放入 `src/lib/`，可复用响应式逻辑放入 `src/composables/`。
- 不提交构建产物、临时文件、编辑器缓存、系统文件、密钥或用户数据。

## 新增工具的要求

新增工具时，应同时完成以下事项：

1. 在 `src/tools/<tool-name>/` 创建独立工具模块。
2. 在路由配置中登记工具页面，路由名和路径保持稳定。
3. 在首页工具清单中添加名称、说明、分类、图标和路由。
4. 在 `README.md` 的工具列表中登记用途和访问地址。
5. 使用统一的 `ToolLayout`、输入区、操作区和结果区布局。
6. 提供清晰的输入、操作、输出、空状态和错误提示。
7. 提供返回首页的入口，并验证 GitHub Pages 下的完整路径。

## UI 设计系统

整体采用“现代、克制、轻量的效率工具”风格，不做成传统后台管理系统或密集导航站。

- 优先使用 shadcn-vue 组件；需要无样式交互原语时使用 Reka UI。
- 图标统一使用 `@lucide/vue`，不混用多套图标库。
- Tailwind CSS 负责布局和样式，避免在组件中堆积大量重复的手写 CSS。
- 使用 CSS Variables 和语义化设计 Token 管理颜色、圆角、阴影及深色模式。
- 品牌主色固定为靛蓝色 `#4F46E5`，背景使用冷灰白，卡片使用细边框和轻阴影。
- 卡片圆角保持在 `12px–16px` 的统一视觉范围。
- 支持浅色、深色和跟随系统三种主题模式。
- 动画应短促、克制，并尊重 `prefers-reduced-motion`。

统一颜色 Token：

| 用途       | 浅色模式  | 深色模式  |
| ---------- | --------- | --------- |
| 品牌主色   | `#4F46E5` | `#818CF8` |
| 主色悬停   | `#4338CA` | `#A5B4FC` |
| 主色浅背景 | `#EEF2FF` | `#1E1B4B` |
| 页面背景   | `#F8FAFC` | `#09090B` |
| 卡片背景   | `#FFFFFF` | `#18181B` |
| 主要文字   | `#0F172A` | `#FAFAFA` |
| 次要文字   | `#64748B` | `#A1A1AA` |
| 边框       | `#E2E8F0` | `#27272A` |
| 成功       | `#16A34A` | `#4ADE80` |
| 警告       | `#D97706` | `#FBBF24` |
| 错误       | `#DC2626` | `#F87171` |

- 主要按钮、文本链接、选中态和键盘焦点统一使用品牌主色。
- 分类图标可以使用辅助色，但不得改变主要交互的品牌识别色。

推荐的首页结构：

- 顶部导航：品牌、搜索、GitHub 链接、主题切换。
- Hero：项目说明、全局搜索、浏览器本地处理提示。
- 分类筛选：全部、文本、开发、图片、数据、时间等。
- 响应式工具卡片网格：图标、名称、简介和分类标签。

推荐的工具页结构：

- 面包屑或返回首页入口。
- 工具标题、用途说明和隐私提示。
- 输入或文件上传区域。
- 主要操作按钮区域。
- 输出结果及复制、下载、清空等操作区域。
- 简短的使用说明和边界说明。

## 界面与体验

- 优先使用语义化 HTML，并为表单元素提供可见标签。
- 所有交互必须支持键盘操作，并保留清晰的焦点状态。
- 保证合理的颜色对比度，不只依靠颜色表达状态。
- 移动端最小宽度按 320px 考虑，主要操作不得依赖悬停。
- 移动端优先使用原生选择控件；复杂交互才使用自定义 Select。
- 空状态、成功状态、失败状态和不可用状态必须清楚且可理解。
- 对耗时操作提供进行中反馈，并在执行期间防止重复触发。
- 不为了“好看”滥用渐变、玻璃拟态、大面积阴影或无意义动画。

## Vue 与 TypeScript 约定

- Props、Emits、工具输入输出和共享数据结构必须定义明确类型。
- 不使用 `any` 逃避类型检查；确实未知的数据先使用 `unknown` 并进行收窄。
- 组件保持单一职责，较复杂的业务逻辑提取为 composable 或纯函数。
- 避免不必要的深层响应式对象、watch 和全局状态。
- 列表渲染必须使用稳定且有意义的 `key`。
- 不直接修改 Props，不在模板中编写复杂业务表达式。
- 用户输入、JSON 解析和文件处理必须校验，并使用 `try...catch` 处理失败情况。
- 不使用 `eval`、动态执行用户输入或不安全的 HTML 拼接。
- 展示用户内容时默认使用 Vue 文本插值；不得对用户输入使用 `v-html`。

## 安全与隐私

- 默认所有数据只在当前浏览器中处理。
- 不把用户输入、文件内容或处理结果上传到第三方。
- 不加入统计、广告、追踪器或远程日志，除非用户明确要求。
- 不提交 API Key、Token、Cookie、账号信息或 `.env` 文件。
- 外部链接使用新窗口打开时，添加 `rel="noopener noreferrer"`。
- 引入第三方依赖前，先评估是否能用浏览器原生 API 或现有依赖实现。
- 必须使用外部服务时，要在实现前说明用途、数据流向、风险和替代方案。

## GitHub Pages 与构建

- Vite 的 `base` 必须配置为 `/macrossx-tools/`，除非部署目标发生变化。
- Vue Router 默认使用 `createWebHashHistory()`。
- 所有资源路径使用 Vite 导入、`import.meta.env.BASE_URL` 或项目内相对路径。
- 禁止写死本机绝对路径或假设部署在 `/`。
- 发布内容来自生产构建产物，不能把开发服务器当作已发布结果。
- 修改部署配置时，同时验证 GitHub Actions、Pages 来源和最终公开地址。

## 修改范围

- 只修改当前任务所需文件，避免顺手重构无关工具。
- 保留已有工具的路由、行为和用户数据兼容性。
- 不删除、重命名或移动现有工具，除非用户明确要求。
- 不创建或切换 Git 分支，除非用户明确要求并确认。
- 不自动提交、推送或发布；只有在用户明确要求时执行，并报告实际结果。

## 验证清单

完成修改后至少检查：

- TypeScript 类型检查通过。
- ESLint、格式化检查和 `git diff --check` 通过。
- Vite 生产构建成功。
- 浏览器控制台没有新增错误。
- 工具核心流程、清空/重置、复制/下载及错误输入均可正常处理。
- 首页搜索、分类、工具入口和返回首页功能有效。
- 键盘操作、焦点状态、浅色模式和深色模式可用。
- 桌面端与 320px 宽度的移动端布局可用。
- 生产构建能通过 `/macrossx-tools/` 子路径加载资源和路由。
- 没有意外文件、敏感信息或用户数据进入变更。

## 交付说明

汇报时简要说明：

- 新增或修改了什么。
- 影响了哪些工具、组件或路由。
- 执行了哪些验证及结果。
- 是否已提交、推送或发布；未执行时明确写明。
